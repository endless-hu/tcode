#include "ffdItem.hh"

int FFDItemCentricProductAllocator::allocate(std::vector<Item>& items
                                             __attribute__((unused))) {
  return 0;
}

int FFDItemCentricSumAllocator::allocate(std::vector<Item>& items
                                         __attribute__((unused))) {
  return 0;
}